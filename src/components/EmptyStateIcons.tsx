import React from 'react';

const EmptyStateIcons: React.FC = () => {
  return (
    <img 
      src="/empty-state-pattern.png"
      alt="Empty state decorative pattern"
      className="absolute right-0 top-0 h-full w-auto opacity-50"
    />
  );
};

export default EmptyStateIcons; 